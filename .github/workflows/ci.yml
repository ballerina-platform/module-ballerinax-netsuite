name: CI

on: [push]

jobs:
    build:

        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2
            - name: Ballerina Build
              uses: SanduDS/ballerina-action/@swan-lake-connector-release
              with:
                  args:
                      build -c --skip-tests
